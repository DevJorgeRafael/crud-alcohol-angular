export interface Empleado {
    id: string;
    nombre: string;
    correo: string;
    telefono: string;
}

export interface Alcohol {
    id: string;
    nombre: string;
    precio: string;
    cantidad: string;
}